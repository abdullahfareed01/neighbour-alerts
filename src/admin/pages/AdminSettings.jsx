import { Settings } from "lucide-react";
import AdminPlaceholder from "../components/AdminPlaceholder";

export default function AdminSettings() {
  return (
    <AdminPlaceholder
      icon={Settings}
      title="Settings"
      description="Admin account and application settings will be implemented in a later phase."
    />
  );
}
